name: PR test
on: pull_request
jobs:
  deploy_source:
    name: build and deploy lambda
    strategy:
      matrix:
        node-version: [12.x]
    runs-on: ubuntu-latest
    steps:
        - name: Run Tests
          run: echo "Aquí correrías tus pruebas"
        
